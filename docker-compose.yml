services:
  pi:
    build: .  
    working_dir: /workspace
    stdin_open: true
    tty: true
    volumes:
      - .:/workspace
      - ~/.pi:/root/.pi
      - ~/.ssh:/root/.ssh:ro
      - ~/.gitconfig:/root/.gitconfig:ro
      - ~/Library/Application Support/glab-cli:/root/.config/glab-cli:ro
